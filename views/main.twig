{% extends "__layout.twig" %}
{% import "macro.html.twig" as forms %}

{% block content %}
<div class="row">
    {% for object in agents_objects %}

    <div class="col-auto border mb-3 mt-3 me-3">
        <div class="mb-1 mt-3">
            <img src="{{ object.image }}" style="width: 200px;">
        </div>

        <div class="d-grid nav-link">
            <a href="/agents-object/{{ object.id }}" class="btn btn-dark" role="button" data-bs-toggle="button">{{ object.title }}</a>
        </div>

        <div class="nav-link">
            <a href="/agents-object/{{ object.id }}" class="btn btn-light" role="button" data-bs-toggle="button">Картинка</a>
            <a href="/agents-object/{{ object.id }}" class="btn btn-light" role="button" data-bs-toggle="button">Описание</a>
        </div>
    </div>
    {% endfor %}
</div>



<!-- Название: {{ object.title }} <br>  
<pre>
    {{ dump(agents_objects) }} 
</pre>

<ul class="list-group">
    <li class="list-group-item">
        <a> {{ forms.btn("Рейна", "/reyna", true, "warning") }} </a>
        <a> {{ forms.btn("Картинка", "/reyna/image", true, "warning") }} </a>
        <a> {{ forms.btn("Описание", "/reyna/info", true, "warning") }} </a>
    </li>

    <li class="list-group-item">
        <a> {{ forms.btn("Киллджой", "/killjoy", true, "warning") }} </a>
        <a> {{ forms.btn("Картинка", "/killjoy/image", true, "warning") }} </a>
        <a> {{ forms.btn("Описание", "/killjoy/info", true, "warning") }} </a>
    </li>
</ul>
 -->

{% endblock %}